export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum ProductType {
  WEIGHT = "WEIGHT",
  VOLUME = "VOLUME",
  COUNT = "COUNT",
}

export enum Unit {
  G = "g",
  KG = "kg",
  ML = "mL",
  L = "L",
  ITEM = "item",
}

export enum OrderStatus {
  PENDING = "PENDING",
  CONFIRMED = "CONFIRMED",
  CANCELLED = "CANCELLED",
}

export enum QuotationStatus {
  DRAFT = "DRAFT",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
}